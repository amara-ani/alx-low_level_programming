#include "main.h"
/**
 * reverse_array - reverses an array
 * @a: checker
 * @n: size
 * Return: void
 */
void reverse_array(int *a, int n)
{
	int i, b;

	for (i= 0; i < n; i++)
	{
	}
	for (b = i - 1; a[b] != '\0'; i--)
	{
	}
}
