#include "main.h"
/**
 * print_line - prints a straight line till the desired input
 * @n: checker
 * Return: 0 (success)
 */
void print_line(int n)
{
	int i;

	i = 0;
	while (++n)
	{
		if (n <= 0 && i > n)
		{
			_putchar('\n');
			break;
		}
		_putchar('_');
	}
	_putchar('\n');
}
		
