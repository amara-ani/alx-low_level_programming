#include "main.h"
#include <stdio.h>
/**
 * mul - muliplies two integers
 * @a: checker
 * @b: checker
 * Return: 0 (success)
 */
int mul(int a, int b)
{
	int result;

	result = a *  b;
	printf("%d", result);
	return (0);
}
