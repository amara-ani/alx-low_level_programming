#include <stdio.h>
/**
 * main - Entry point
 * printf(): to display result
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
