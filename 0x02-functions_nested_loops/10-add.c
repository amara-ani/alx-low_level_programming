#include "main.h"
/**
 * add - prints addition of two numbers
 * @a: checker
 * @b: another checker
 * Return: values
 */
int add(int a, int b);
{
	int a, b, result;

	result = a + b;
	return result;
}
