#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _islower - checks if its a lower caseor uppercase
 * Return: 0 (success)
 */
int _islower(int c)
{
	if (_islower(c) == c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
